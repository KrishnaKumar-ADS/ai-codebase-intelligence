[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --cov=. --cov-report=term-missing -m "not integration"
markers =
	integration: marks tests requiring a live Docker production stack